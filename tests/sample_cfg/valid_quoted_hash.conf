server {
    listen 8091;
    server_name quoted.example.com;
    root /var/www/quoted;
    index "index#special.html"; # this trailing comment must still be stripped
    location / {
        root /var/www/quoted;
    }
}
